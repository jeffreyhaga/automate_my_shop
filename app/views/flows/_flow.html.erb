<div id="<%= dom_id flow %>">
  <p class="card-title">
    <strong>Title:</strong>
    <%= flow.title %>
  </p>

  <p class="card-text">
    <strong>Description:</strong>
    <%= flow.description %>
  </p>

  <p class="card-text">
      <% if flow.file.attached? %>
        <span>📎</span> <%= link_to 'Download File', rails_blob_path(flow.file, disposition: 'attachment') %>
      <% else %>
        <span>No file attached.</span>
      <% end %>
    </p>

</div>
