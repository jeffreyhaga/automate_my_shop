<p style="color: green"><%= notice %></p>

<h1>Contact</h1>
<p>Need help with your shop or have a question? <br> Fill out the form below and we'll get back to you as soon as possible.</p>

<%= form_with(model: @contact, url: contact_path, local: true) do |f| %>
<div class="mt-1 col-md-6">
    <%= f.label :name, class: "form-label" %>
    <%= f.text_field :name, class: "form-control", required: true %>
</div>
<div class="mt-1 col-md-6">
    <%= f.label :email, class: "form-label" %>
    <%= f.email_field :email, class: "form-control me-2 w-100 w-md-50 w-lg-25", required: true %>
</div>
<div class="mt-1 col-md-6">
    <%= f.label :message, class: "form-label" %>
    <%= f.text_area :message, class: "form-control", required: true %>
</div>
<div class="mt-3">
    <%= f.submit "Send Message", class: "btn btn-primary" %>
</div>
<% end %>
